# گزارش موقت: تاریخچه پیگیری و افزودن توضیحات در پنل مشتری

> فایل موقتی برای بررسی قبل از فیکس — بعد از رفع مشکلات قابل حذف است.

---

## خلاصه

بخش «تاریخچه پیگیری» و «افزودن توضیحات جدید» در مودال پنل مشتری (`#detailModal`) روی جدول `followups` کار می‌کند.

---

## محل کد

| بخش | فایل | خطوط تقریبی |
|-----|------|-------------|
| شل مودال پنل | `index.html` | ~1009–1021 |
| رندر تاریخچه + فرم افزودن | `src/customers.js` → `openCustomerDetail` | ~1581–1648 |
| ثبت توضیح سریع | `src/customers.js` → `addQuickNote` | ~1732–1758 |
| INSERT پیگیری | `src/data.js` → `saveFollowupToDB` | ~794–836 |
| مودال پیگیری جداگانه | `index.html` + `src/followups.js` → `saveFollowup` | ~808–846 / ~475–533 |
| دسترسی ثبت یادداشت | `src/utils.js` → `canAddNoteOnCustomer` | ~1007–1014 |
| استایل تایم‌لاین | `src/styles.css` | ~863–977 |

---

## جریان کار فعلی

```
باز کردن پنل مشتری
  → filter روی data.followups با customerId
  → sort نزولی بر اساس تاریخ
  → رندر تایم‌لاین (+ دکمه‌های ویرایش/حذف)
  → فرم «افزودن توضیحات جدید» (اگر دسترسی باشد)

ثبت توضیح
  → addQuickNote (با try/catch)
  → تاریخ = امروز جلالی (عمدی)
  → type از select (دایرکت/تماس/کامنت/پیام)
  → saveFollowupToDB → INSERT
  → push به cache و باز کردن مجدد پنل
```

### گزینه‌های کانال (type)

- دایرکت
- تماس
- کامنت
- پیام

### گزینه‌های نتیجه (result)

پاسخ داد، صحبت شد، ارسال قیمت، ارسال اطلاعات، پیگیری، پاسخ نداد، Happy Customer ❤️

---

## مشکلات

### 1. قفل soft-duplicate روی `(customer_id, date, type)` — ✅ فیکس شد

قبلاً در یک روز شمسی، برای هر مشتری فقط یک رکورد با همان `type` قابل ثبت بود.

**فیکس:** چک soft-duplicate از `saveFollowupToDB` حذف شد؛ چند توضیح هم‌نوع در یک روز مجاز است.

---

### 2. `addQuickNote` بدون `try/catch` — ✅ فیکس شد

قبلاً خطای DB/شبکه silent بود.

**فیکس:** مثل `saveFollowup`، خطا catch می‌شود و با toast نمایش داده می‌شود.

---

### 3. تایم‌لاین بدون مرتب‌سازی زمانی — ✅ فیکس شد

قبلاً ترتیب = ترتیب آرایه در حافظه بود؛ «آخرین پیگیری» در لیست هم آخرین عنصر آرایه بود نه آخرین تاریخ.

**فیکس:** `sortFollowupsNewestFirst` — تاریخ نزولی (+ tie-break روی `id`) در تایم‌لاین پنل و ستون آخرین پیگیری لیست مشتریان.

---

### 5. عدم ویرایش/حذف از داخل پنل — ✅ فیکس شد

قبلاً تایم‌لاین فقط نمایش بود.

**فیکس:** دکمه‌های ویرایش/حذف در هر آیتم تایم‌لاین (با همان permissionهای تب پیگیری‌ها). بعد از ذخیره/حذف از مودال، اگر پنل مشتری باز باشد دوباره رندر می‌شود.

---

## موارد باقی‌مانده / خارج از اسکوپ این فیکس

| مورد | وضعیت |
|------|--------|
| تاریخ همیشه «امروز» در فرم سریع پنل | عمدی — نیاز به فیکس نیست |
| نبود unique constraint در DB | P3 / اختیاری |
| فیلد `customers.notes` جدا از تایم‌لاین | گیج‌کننده ولی باگ این بخش نیست |
| ناهماهنگی با یادداشت‌های «انجام‌شده» | دیگر موضوعیت ندارد (قفل روزانه برداشته شد) |

---

## فایل‌های تغییرکرده در فیکس

1. `src/data.js` — حذف soft-duplicate
2. `src/customers.js` — sort، try/catch، دکمه‌های ویرایش/حذف در تایم‌لاین
3. `src/followups.js` — رفرش پنل مشتری بعد از ذخیره/حذف
4. `src/styles.css` — استایل `.timeline-actions`
