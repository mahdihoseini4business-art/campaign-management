# پلن پیاده‌سازی: ثبت هدیه با قیمت ۰

## خلاصه تصمیم محصول

مسیر هدیه **سوییچ جدا یا CTA جدا نیست**. کاربر در همان فرم ثبت فروش، در فیلد «قیمت کل» مقدار `0` وارد می‌کند. سیستم از روی همین سیگنال تشخیص می‌دهد که فروش **هدیه** است، فیلدهای واریز را مخفی می‌کند، و بدون دریافت وجه ثبت را می‌پذیرد — به‌شرطی که محصول در کاتالوگ برای هدیه مجاز باشد.

### قوانین طلایی (الزامی)

1. کاربر از اول بداند دارد **هدیه** ثبت می‌کند، نه فروش عادی با قیمت اشتباه.
2. سیستم قیمت صفر و «بدون واریز» را برای هدیه **عمداً** بپذیرد، نه با هک.
3. در همه جا (لیست فروش، حسابداری، ماتریس، LRFM) هدیه با **برچسب واضح** دیده شود.

### قوانین کسب‌وکار اضافه‌شده توسط این طرح

4. فقط محصولاتی که ادمین در **تنظیمات کاتالوگ** اجازه هدیه داده باشد، می‌توانند قیمت `0` بگیرند.
5. برای محصول غیرمجاز، ورود `0` رد شود با پیام روشن (نه سکوت و نه قبول به‌عنوان فروش عادی).

---

## ۱. وضعیت فعلی (نقاط شکست)

| لایه | رفتار امروز | اثر روی هدیه |
|---|---|---|
| `applySaleProductDraft` | فقط `price > 0` را می‌نویسد | صفر ذخیره نمی‌شود |
| `commitSalePayment` | `priceNum <= 0` و `amountNum <= 0` invalid | ثبت ممکن نیست |
| `isProductPriceLocked` | قفل اگر `price > 0` یا `priceLocked` | صفر قفل مفهومی ندارد |
| `syncProductStatus` | بیعانه/تکمیل بر اساس واریز تأییدشده | وضعیت هدیه وجود ندارد |
| `getAllPayments` | واریز با `amount <= 0` را رد می‌کند | صف حسابداری خالی می‌ماند |
| `saleLineHasApprovedPayment` | نیاز به واریز تأییدشده با مبلغ > ۰ | مالکیت ساخته نمی‌شود |
| کاتالوگ | فقط `name`, `productKind`, `costAmount` | فلگ اجازه هدیه نیست |

فایل‌های کلیدی: `src/customers.js`, `src/utils.js`, `src/data.js`, `src/accounting.js`, `src/sales.js`, `src/auth.js`, `index.html`.

---

## ۲. تجربه کاربری نهایی (روند کامل)

### ۲.۱ پیش‌نیاز ادمین — کاتالوگ

مسیر: تنظیمات → کاتالوگ محصولات.

روی هر ردیف محصول (حالت نمایش و ویرایش):

- چک‌باکس / سوییچ: **«قابل ثبت به عنوان هدیه»**
- راهنمای کوتاه زیر لیست:  
  > فقط محصولاتی که این گزینه برایشان روشن است، در فرم فروش می‌توانند با قیمت ۰ به‌عنوان هدیه ثبت شوند.

مقادیر پیش‌فرض پیشنهادی هنگام پیاده‌سازی:

- محصولات موجود در کاتالوگ: `allowGift: false` (امن‌ترین حالت؛ ادمین عمداً باز کند)
- محصول جدید: همان `false` مگر ادمین موقع افزودن روشن کند

باندل‌ها: در فاز ۱ هدیه فقط روی **نام محصول کاتالوگ** مجاز است، نه روی باندل به‌عنوان یک واحد فروش. اگر بعداً لازم شد، جداگانه تصمیم گرفته می‌شود.

### ۲.۲ فروشنده — ثبت هدیه

```
+ ثبت فروش → انتخاب/باز بودن مشتری → تب فروش‌ها → ردیف جدید
        ↓
۱. انتخاب نام محصول (از لیست قابل فروش)
۲. وارد کردن قیمت کل = 0
        ↓
سیستم لحظه‌ای چک می‌کند:
  - آیا این محصول allowGift دارد؟
        ↓
  ┌─ خیر ─────────────────────────────────────────────┐
  │ فیلد قیمت invalid می‌شود                          │
  │ پیام: «ثبت هدیه برای این محصول مجاز نیست»         │
  │ بخش واریزها سر جایش می‌ماند (حالت فروش عادی)     │
  │ ثبت ممکن نیست تا قیمت > 0 یا محصول مجاز شود      │
  └───────────────────────────────────────────────────┘
        ↓ بله
  ┌─ بله ─────────────────────────────────────────────┐
  │ بانر/بج واضح بالای بلوک: «ثبت هدیه»               │
  │ متن کمکی: «قیمت صفر = هدیه · واریز لازم نیست»     │
  │ قیمت روی ۰ می‌ماند (هنوز قفل نهایی نشده)          │
  │ مرحله واریزها + فیلدهای مبلغ/تاریخ/ساعت/بانک     │
  │   و دکمه «افزودن واریز» ناپدید می‌شوند            │
  │ فیلدهای فیزیکی (آدرس/کدپستی) اگر محصول physical  │
  │   است می‌مانند                                     │
  │ تاریخ تسویه: مخفی (برای هدیه معنا ندارد)          │
  │ دکمه ثبت تبدیل می‌شود به: «ثبت هدیه»              │
  └───────────────────────────────────────────────────┘
        ↓
کاربر «ثبت هدیه» را می‌زند
        ↓
اعتبارسنجی نهایی (نام محصول، قیمت دقیقاً ۰، allowGift،
  آدرس فیزیکی در صورت نیاز)
        ↓
ذخیره خط فروش با saleType=gift، price=0، priceLocked،
  بدون واریز، وضعیت در انتظار تأیید حسابداری
        ↓
toast: «هدیه ثبت شد و در انتظار تأیید حسابداری است.»
```

### ۲.۳ تشخیص زنده در UI (قانون طلایی ۱)

ورود `0` به‌تنهایی کافی نیست که کاربر «متوجه» هدیه شود؛ باید بازخورد فوری و واضح باشد:

| لحظه | بازخورد |
|---|---|
| تایپ `0` روی محصول مجاز | بج «هدیه»، مخفی شدن واریزها، تغییر لیبل دکمه |
| تایپ `0` روی محصول غیرمجاز | خطای فیلد + توضیح؛ واریزها مخفی نشوند |
| پاک کردن `0` / گذاشتن قیمت > ۰ | برگشت فوری به فرم فروش عادی + نمایش دوباره واریزها |
| عوض کردن نام محصول بعد از `0` | دوباره ارزیابی `allowGift`؛ اگر دیگر مجاز نیست، خطا |

پیشنهاد کپی UI:

- بج: `هدیه`
- راهنما: `قیمت صفر یعنی این فروش به‌عنوان هدیه ثبت می‌شود.`
- خطای غیرمجاز: `ادمین اجازه ثبت هدیه برای «{نام محصول}» را نداده است. قیمت را بزرگ‌تر از صفر وارد کنید یا با ادمین هماهنگ کنید.`

### ۲.۴ حسابداری

هدیه **واریز پولی ندارد**، پس نباید در صف واریزهای مبلغ‌دار گم شود.

پیشنهاد صف:

- همان شیت حسابداری، با فیلتر/تب اضافه: **همه | در انتظار | تأییدشده | ردشده | هدایا**
- یا کارت‌های هدیه داخل «در انتظار» با بج رنگی `هدیه` و مبلغ `۰`

اقدامات:

| دکمه | معنی |
|---|---|
| تأیید ثبت هدیه | مالکیت محصول برای مشتری فعال می‌شود |
| رد | با دلیل؛ هدیه مالکیت نمی‌دهد |

متن کارت: نام مشتری، محصول، ثبت‌کننده، تاریخ ثبت، بج هدیه، مبلغ ۰.

### ۲.۵ نمایش بعد از ثبت (قانون طلایی ۳)

| محل | نمایش |
|---|---|
| تب فروش‌های مشتری | ردیف با بج `هدیه`، مبلغ `۰`، وضعیت `هدیه` / `در انتظار تأیید` / `ردشده` |
| شیت فروش‌ها | ستون/بج هدیه؛ فیلتر «فقط هدایا» و «بدون هدایا»؛ مبلغ `۰` |
| آمار درآمد / داشبورد | **از جمع پول حذف**؛ متریک جدا اختیاری: تعداد هدیه |
| ماتریس محصول | بعد از تأیید، مثل داشتن محصول؛ tooltip یا نشان `هدیه` |
| LRFM | مبلغ خرید (M): بدون هدیه؛ شمارش مالکیت محصول: با هدیه تأییدشده (پیشنهاد پیش‌فرض) |
| ارسالی‌ها | اگر محصول physical و نیاز به ارسال دارد، بعد از تأیید در جریان ارسال قابل دیده‌شدن باشد (فاز ۱.۵ در صورت نیاز فعلی پنل) |

---

## ۳. مدل داده

### ۳.۱ کاتالوگ — `app_settings.product_catalog`

شکل فعلی هر آیتم:

```js
{ name, productKind: 'educational' | 'physical', costAmount? }
```

افزوده می‌شود:

```js
{ name, productKind, costAmount?, allowGift: boolean }
```

نرمال‌سازی در `normalizeCatalogEntry` / `normalizeProductCatalog`:

- اگر `allowGift` نبود → `false`
- ذخیره از طریق همان `saveProductCatalog` / `saveSetting('product_catalog', …)`

API کمکی پیشنهادی در `data.js`:

```js
isProductGiftAllowed(productName) → boolean
// نام را با کاتالوگ match کند؛ باندل در فاز ۱ false
```

### ۳.۲ خط فروش — `customers.products[]`

افزوده/تغییر:

```js
{
  name: string,
  saleType: 'standard' | 'gift',  // پیش‌فرض برای داده‌های قدیم: standard
  price: '0',                     // برای هدیه همیشه 0
  priceLocked: true,              // بعد از ثبت هدیه
  status: 'هدیه',                 // به‌جای بیعانه/تکمیل
  giftAccountingStatus: 'pending' | 'approved' | 'rejected',
  giftRejectReason?: string,
  giftReviewedAt?: string,
  giftReviewedBy?: string,
  soldByPhone, soldAt,            // soldAt = زمان ثبت هدیه
  payments: [],                   // برای هدیه خالی می‌ماند
  // shipping* در صورت physical مثل قبل
}
```

نکته طراحی:

- برای فروش عادی همان `payments[].paymentStatus` می‌ماند.
- برای هدیه، چون واریزی نیست، وضعیت تأیید در سطح **خط محصول** با `giftAccountingStatus` نگه داشته می‌شود تا به صف حسابداری جعلی (واریز ۰ تومان) نیاز نباشد.

Migration داده: لازم نیست DB migration جدا؛ JSONB مشتری و settings است. فقط نرمال‌سازی هنگام خواندن:

- نبود `saleType` → `standard`
- نبود `allowGift` در کاتالوگ → `false`

---

## ۴. قواعد اعتبارسنجی و state ماشین

### ۴.۱ تشخیص حالت پیش‌نویس در فرم

```
draftPrice = parseFloat(priceInput)
isGiftDraft = draftPrice === 0 && productName انتخاب شده

if (isGiftDraft && !isProductGiftAllowed(name)) → error, not gift mode
if (isGiftDraft && isProductGiftAllowed(name))  → gift UI mode
if (draftPrice > 0)                             → standard UI mode
if (price خالی)                                 → حالت خنثی (هنوز تصمیم گرفته نشده)
```

مهم: `price` خالی ≠ هدیه. فقط **مقدار عددی صفر** هدیه است.

### ۴.۲ ثبت هدیه (جایگزین مسیر commit واریز)

تابع جدید پیشنهادی: `commitGiftSale(customerId, productIndex)` یا شاخه داخل همان submit وقتی `isGiftMode`.

شرایط موفقیت:

1. نام محصول غیرخالی و در لیست قابل‌فروش
2. `parseFloat(price) === 0`
3. `isProductGiftAllowed(name) === true`
4. اگر physical: آدرس/کدپستی طبق قواعد فعلی (اگر برای فروش عادی الزامی‌اند، برای هدیه هم همان)
5. کاربر مجاز به ثبت فروش روی این مشتری باشد (`canAddSaleOnCustomer`)

اثرات ذخیره:

- `saleType = 'gift'`
- `price = '0'`, `priceLocked = true`
- `status = 'هدیه'`
- `payments = []`
- `giftAccountingStatus = 'pending'`
- `soldAt` / `soldByPhone` پر شود
- snapshot هزینه کاتالوگ در صورت وجود (اختیاری، مثل فروش عادی)

### ۴.۳ فروش عادی (بدون تغییر مفهومی)

همان قواعد فعلی؛ اضافه:

- اگر کاربر `0` بزند و محصول غیرمجاز باشد → ثبت نشود
- اگر بعداً کسی دستی JSON را دستکاری کند، گارد سمت منطق خواندن/ذخیره: هدیه بدون `allowGift` در زمان ثبت پذیرفته نشود (allowGift فقط در لحظه ثبت چک می‌شود؛ تغییر بعدی کاتالوگ هدیهٔ ازقبل‌ثبت‌شده را باطل نکند)

### ۴.۴ قفل و وضعیت

به‌روزرسانی helpers در `utils.js`:

| تابع | تغییر |
|---|---|
| `isGiftSale(product)` | `saleType === 'gift'` یا (`price === 0` و فلگ ذخیره‌شده) |
| `isProductPriceLocked` | برای هدیه ثبت‌شده هم locked |
| `syncProductStatus` | اگر gift: همیشه `status = 'هدیه'`؛ وارد منطق بیعانه/تکمیل نشود |
| `isInvoiceClosed` | هدیهٔ تأییدشده = بسته از نظر ویرایش |
| `saleLineHasApprovedPayment` یا جایگزین | برای مالکیت: gift با `giftAccountingStatus === 'approved'` هم کافی است |
| `isProductCountableInSales` | هدیه تأییدشده در شمارش تعداد خرید بیاید؛ در جمع مبلغ نه |
| `getApprovedPaid` | برای gift همیشه `0` |

---

## ۵. تغییرات UI به تفکیک سطح

### ۵.۱ تنظیمات کاتالوگ — `index.html` + `src/auth.js`

- در فرم افزودن محصول: کنترل `allowGift`
- در ردیف لیست و مود ویرایش: نمایش/ویرایش `allowGift`
- `renderProductCatalogSettings`, `addProductCatalogItem`, `saveProductCatalogEdit` باید فیلد را بخوانند/بنویسند
- `normalizeCatalogEntry` در `data.js`

### ۵.۲ فرم فروش — `src/customers.js` + استایل

روی `input[data-sale-field="price"]`:

- listener ورودی (همان‌جا که فرمت عدد زده می‌شود): `updateSaleGiftMode(blockEl)`
- `updateSaleGiftMode`:
  - اگر gift mode معتبر: اضافه کردن کلاس مثلاً `is-gift-sale` روی `.product-block`
  - مخفی کردن `.sale-step-payments` و فیلد تاریخ تسویه
  - نشان‌دادن نوار `sale-gift-banner`
  - تغییر `[data-sale-submit]` به «ثبت هدیه» و bind به commit هدیه
  - اگر غیرمجاز: کلاس خطا روی قیمت، banner نشان داده نشود، payments مخفی نشوند

مخفی‌سازی با CSS/`hidden` ترجیح دارد تا DOM واریز پیش‌نویس پاک نشود؛ اگر کاربر از ۰ برگردد، واریزها بدون از دست رفتن draft برگردند. هنگام **ثبت موفق هدیه**، `payments` خالی ذخیره شود.

### ۵.۳ حسابداری — `src/accounting.js`

- منبع صف هدایا: پیمایش `customer.products` جایی که `saleType === 'gift' && giftAccountingStatus === 'pending'`
- UI کارت هدیه + `approveGiftSale` / `rejectGiftSale`
- permission همان `accounting`

### ۵.۴ لیست فروش و آمار — `src/sales.js` (+ داشبورد اگر جمع می‌زند)

- `getAllSales`: فیلد `isGift` / `saleType`
- نمایش بج
- فیلتر هدیه
- جمع مبلغ: gift را صفر بماند و از KPI درآمد جدا شود

### ۵.۵ مالکیت / ماتریس / LRFM — `data.js`, `product-matrix.js`, `utils.js`

- `getCustomerOwnedProductNames`: خطوط gift تأییدشده را شامل کند
- ماتریس: نشان اختیاری منبع هدیه
- `computeCustomerLrfm`: M بدون هدیه؛ شمارش سطوح با سیاست بخش ۲.۵

---

## ۶. جریان حسابداری هدیه (جزئی)

```
ثبت هدیه (فروشنده)
  giftAccountingStatus = pending
  مالکیت هنوز فعال نیست
        ↓
حسابدار در صف می‌بیند (بج هدیه، مبلغ ۰)
        ↓
   تأیید ──→ giftAccountingStatus = approved
             getCustomerOwnedProductNames شامل محصول می‌شود
             ماتریس/سطوح به‌روز
        ↓
   رد ─────→ giftAccountingStatus = rejected + reason
             مالکیت نمی‌آید
             ردیف در تاریخچه با وضعیت ردشده می‌ماند (قابل مشاهده، غیرقابل ویرایش قیمت)
```

ویرایش بعد از ثبت:

- مثل فروش ثبت‌شده: نام/قیمت هدیه قفل
- حذف: طبق سیاست فعلی خطوط ثبت‌شده (اگر حذف فروش عادی ممنوع است، هدیه هم ممنوع مگر ادمین)

---

## ۷. لبه‌ها و تصمیم‌های صریح

| سناریو | تصمیم پلن |
|---|---|
| قیمت خالی | هدیه نیست |
| قیمت `0` بدون انتخاب محصول | هنوز gift mode کامل نه؛ با انتخاب محصول ارزیابی شود |
| محصول غیرمجاز + `0` | خطا؛ ثبت نشود |
| ادمین بعداً `allowGift` را خاموش کند | هدایای قبلی معتبر بمانند؛ فقط ثبت جدید بسته شود |
| باندل | فاز ۱: هدیه مجاز نیست (`allowGift` فقط روی محصول ساده) |
| چند هدیه همان محصول به یک مشتری | مجاز؛ هشدار نرم «این مشتری از قبل این محصول را دارد» |
| هدیه و همزمان واریز | غیرممکن در UI؛ payments خالی اجباری |
| گزارش درآمد | هدیه در جمع تومان نیاید |
| سطح LRFM بر اساس تعداد | هدیه تأییدشده شمرده شود (قابل تغییر با یک ثابت/`settings` بعداً) |
| کاربر قیمت را `۰` بگذارد بعد به فروش عادی برگرداند قبل از ثبت | OK؛ UI برگردد |
| ثبت هدیه با دسترسی فروش روی مشتری دیگران | همان `canAddSaleOnCustomer` |

---

## ۸. فازبندی پیاده‌سازی

### فاز ۰ — آماده‌سازی داده و helperها

1. `allowGift` در نرمال‌سازی کاتالوگ + UI تنظیمات + ذخیره
2. `isProductGiftAllowed(name)`
3. `isGiftSale(product)` و نرمال‌سازی `saleType` روی خواندن مشتری
4. به‌روزرسانی `syncProductStatus` / مالکیت / `getApprovedPaid` برای gift (هنوز UI فروش کامل نیست)

**خروجی:** ادمین می‌تواند محصولات را برای هدیه علامت بزند؛ منطق پایه آماده است.

### فاز ۱ — فرم فروش + ثبت

1. `updateSaleGiftMode` روی تغییر قیمت/نام
2. مخفی‌سازی واریزها + بج + کپی خطا
3. `commitGiftSale` و دکمه «ثبت هدیه»
4. گارد: محصول غیرمجاز نتواند ۰ ثبت کند
5. نمایش ردیف هدیه در تب فروش مشتری

**خروجی:** فروشنده می‌تواند هدیه مجاز ثبت کند؛ در انتظار تأیید می‌ماند.

### فاز ۲ — حسابداری + مالکیت واقعی

1. صف/فیلتر هدایا در حسابداری
2. approve/reject هدیه
3. اتصال به `getCustomerOwnedProductNames` و ماتریس
4. LRFM و آمار فروش: حذف از درآمد، برچسب در لیست

**خروجی:** چرخه کامل از ثبت تا مالکیت تأییدشده.

### فاز ۳ — پولیش

1. فیلتر «فقط هدایا» در شیت فروش‌ها
2. هشدار تکریری بودن محصول
3. متریک «تعداد هدیه» در داشبورد (اختیاری)
4. تست دستی سناریوهای بخش ۷

---

## ۹. چک‌لیست فایل‌ها

| فایل | کار |
|---|---|
| `src/data.js` | `allowGift` در normalize/save؛ `isProductGiftAllowed`؛ مالکیت gift تأییدشده |
| `src/auth.js` + `index.html` | UI تنظیمات کاتالوگ |
| `src/utils.js` | `isGiftSale`, وضعیت، قفل قیمت، countable، LRFM M |
| `src/customers.js` | gift mode UI، مخفی‌سازی واریز، `commitGiftSale`، رندر بج |
| `src/accounting.js` | صف و تأیید/رد هدیه |
| `src/sales.js` | بج، فیلتر، جمع‌ها |
| `src/product-matrix.js` | منبع مالکیت + نشان هدیه |
| استایل مرتبط (CSS موجود پروژه) | `.is-gift-sale`, banner، badge |
| `UX-gift-sales.md` | همین سند به‌عنوان مرجع پلن |

---

## ۱۰. معیار پذیرش (Definition of Done)

1. ادمین بتواند روی هر محصول کاتالوگ «قابل هدیه» را روشن/خاموش کند و ذخیره شود.
2. در فرم فروش، وارد کردن `0` برای محصول مجاز → بج هدیه، محو شدن فیلدهای واریز، دکمه «ثبت هدیه».
3. وارد کردن `0` برای محصول غیرمجاز → خطا، بدون ورود به حالت هدیه، بدون امکان ثبت.
4. ثبت هدیه بدون هیچ واریزی موفق شود و `price = 0` ذخیره شود.
5. هدیه در حسابداری با برچسب مشخص دیده و قابل تأیید/رد باشد.
6. بعد از تأیید، محصول در مالکیت مشتری / ماتریس دیده شود.
7. مبلغ هدیه در درآمد/جمع فروش‌های پولی نیاید.
8. در لیست فروش و تب مشتری، برچسب «هدیه» واضح باشد.
9. برگشت از قیمت ۰ به قیمت مثبت قبل از ثبت، فرم را به حالت عادی برگرداند.

---

## ۱۱. جمع‌بندی روند برای ذی‌نفعان

> ادمین در کاتالوگ مشخص می‌کند کدام محصول را می‌شود هدیه داد. فروشنده همان فرم فروش را باز می‌کند، محصول را می‌زند و قیمت کل را ۰ می‌گذارد. اگر مجاز باشد، سیستم خودش می‌فهمد «هدیه است»، واریزها را برمی‌دارد و ثبت هدیه را می‌گیرد. حسابداری هدیه را مثل یک درخواست مالکیت بدون وجه تأیید می‌کند. بعد از تأیید، مشتری آن محصول را دارد و همه‌جا با برچسب هدیه دیده می‌شود — بدون قیمت جعلی و بدون واریز ساختگی.
