# ثبت فروش هدیه — پیشنهاد UX

هدف: وقتی محصولی **رایگان به مشتری می‌دهیم**، بتوانیم آن را طوری ثبت کنیم که:

1. در پنل مشخص باشد مشتری این محصول را **دارد** (مالکیت / سابقه خرید برای ماتریس محصول و LRFM)
2. قیمت واقعی فروش **صفر** باشد (چون پولی گرفته نشده)
3. حسابداری، گزارش‌ها و قفل قیمت گیج نشوند
4. مسیر ثبت برای فروشنده کوتاه و اشتباه‌نکردنی باشد

---

## ۱. مشکل فعلی

الان فروش فقط وقتی «ثبت» می‌شود که:

- قیمت کل **بزرگ‌تر از صفر** باشد
- حداقل یک واریز با مبلغ **> ۰**، تاریخ، ساعت و بانک مقصد پر شود
- بعد از تأیید حسابداری، محصول در لیست «محصولات مالک» مشتری دیده شود

برای هدیه این منطق می‌شکند:

| نیاز کسب‌وکار | محدودیت سیستم |
|---|---|
| قیمت باید صفر باشد | `priceNum <= 0` رد می‌شود |
| پولی واریز نشده | واریز هم باید > ۰ باشد |
| مشتری باید محصول را «داشته باشد» | مالکیت فقط از واریز **تأییدشده** می‌آید |
| نباید مثل فروش عادی در آمار درآمد بیاید | الان هر فروش تأییدشده در آمار واریز/فروش می‌رود |

راه‌حل موقت (مثلاً قیمت ۱ تومان یا واریز ساختگی) هم برای UX بد است، هم گزارش‌ها را خراب می‌کند، هم حسابداری را گیج می‌کند.

---

## ۲. اصل طراحی پیشنهادی

> **هدیه یک نوع فروش است، نه یک میان‌بر برای دور زدن قیمت صفر.**

یعنی همان موجودیت `products[]` روی مشتری می‌ماند (تا مالکیت و تاریخچه یکپارچه بماند)، ولی با یک فلگ صریح مثل `saleType: 'gift' | 'standard'` و قواعد متفاوت برای قیمت، واریز و گزارش.

سه قانون طلایی UX:

1. کاربر از اول بداند دارد **هدیه** ثبت می‌کند، نه فروش عادی با قیمت اشتباه.
2. سیستم قیمت صفر و «بدون واریز» را برای هدیه **عمداً** بپذیرد، نه با هک.
3. در همه جا (لیست فروش، حسابداری، ماتریس، LRFM) هدیه با برچسب واضح دیده شود.

---

## ۳. ایده‌ها (از پیشنهادی تا جایگزین)

### ایده A — سوییچ «فروش هدیه» داخل همان فرم ثبت فروش ✅ پیشنهاد اصلی

در مرحلهٔ «۱. محصول»، کنار نام محصول یک کنترل واضح:

```
○ فروش عادی    ● فروش هدیه
```

یا یک toggle با برچسب:

> 🎁 این فروش هدیه است (بدون دریافت وجه)

**رفتار وقتی هدیه روشن است:**

| فیلد | رفتار |
|---|---|
| قیمت کل | خودکار `0`، غیرفعال (read-only)، با متن کم‌رنگ: «قیمت هدیه صفر است» |
| بخش واریزها | مخفی یا جایگزین با یک بلوک کوتاه: «واریز لازم نیست» |
| تاریخ هدیه | الزامی (پیش‌فرض: الآن) — معادل تاریخ فروش |
| دلیل / مناسبت هدیه | اختیاری ولی توصیه‌شده (مثلاً کمپین، جبران، VIP، نمونه) |
| دکمه ثبت | «ثبت هدیه» به‌جای «ثبت فروش» |

بعد از ثبت:

- وضعیت پرداخت: **هدیه** (نه بیعانه / تکمیل)
- برای حسابداری: صف جدا یا همان صف با برچسب «هدیه» — تأیید یعنی «تأیید تحویل/ثبت مالکیت»، نه تأیید وجه
- `priceLocked = true` با قیمت ۰
- مالکیت محصول بعد از تأیید حسابداری فعال شود (مثل فروش عادی)

**چرا بهترین UX است؟**

- فروشنده مسیر آشنای فعلی را می‌شناسد؛ فقط یک تصمیم اضافه می‌گیرد.
- اشتباه قیمت ۱ تومانی / واریز جعلی حذف می‌شود.
- زبان UI شفاف است: «ثبت هدیه».
- کمترین سطح مفهوم جدید نسبت به مدل داده فعلی.

---

### ایده B — CTA جدا: «+ ثبت هدیه» کنار «+ ثبت فروش»

دو دکمه در تب فروش‌ها:

- `+ ثبت فروش`
- `+ ثبت هدیه`

مودال/فرم هدیه ساده‌تر است: فقط مشتری، محصول، تاریخ، توضیح. بدون قیمت و واریز.

**مزیت:** کشف‌پذیری بالا؛ کاربر تازه‌کار گیج نمی‌شود که «چطور قیمت را صفر کنم؟»  
**عیب:** دو مسیر موازی؛ اگر فروشنده عادت به یک دکمه دارد، ممکن است هدیه را فراموش کند و دوباره هک قیمت بزند.

**ترکیب پیشنهادی با A:** دکمهٔ جدا برای کشف‌پذیری + داخل فرم فروش هم سوییچ هدیه برای کسانی که از مسیر عادی آمده‌اند.

---

### ایده C — قیمت کاتالوگ + تخفیف ۱۰۰٪ (مدل حسابداری‌پسند)

به‌جای قیمت صفر خام:

- `listPrice` = قیمت رسمی کاتالوگ (برای گزارش ارزش هدیه / هزینه فرصت)
- `discountPercent = 100` یا `discountAmount = listPrice`
- `payable = 0`

در UI:

> قیمت کاتالوگ: ۱۲٬۰۰۰٬۰۰۰  
> تخفیف هدیه: ۱۰۰٪  
> مبلغ قابل پرداخت: ۰

**مزیت:** برای مالی واضح است «چقدر ارزش هدیه داده‌ایم»؛ سود/زیان و گزارش کمپین قابل محاسبه است.  
**عیب:** کمی سنگین‌تر از A؛ اگر فعلاً نیاز به ارزش‌گذاری هدیه ندارید، overkill است.

**پیشنهاد فازبندی:** فاز ۱ = ایده A با `price = 0` و `saleType = gift`؛ فاز ۲ = ذخیره `catalogPriceSnapshot` برای گزارش ارزش هدایا.

---

### ایده D — «تخصیص محصول» جدا از فروش (مالکیت بدون فاکتور)

یک موجودیت جدا مثل `entitlements` / `assignments`:

> مشتری X محصول Y را دارد — منبع: خرید | هدیه | انتقال

فروش فقط برای موارد پولی بماند.

**مزیت:** مدل مفهومی تمیز؛ آمار فروش درآمدی آلوده نمی‌شود.  
**عیب:** ماتریس محصول، LRFM، حسابداری و UI باید دو منبع را بخوانند؛ هزینهٔ پیاده‌سازی و آموزش بالاتر است.

برای این پنل فعلاً توصیه نمی‌شود مگر اینکه هدیه و فروش از نظر فرآیند کسب‌وکار کاملاً جدا شوند (مثلاً انبار/پشتیبانی هدیه بدهد، فروشنده فروش ثبت کند).

---

### ایده E — واریز مجازی بانک «هدیه» (ضدپیشنهاد — انجام ندهید)

ثبت قیمت نمادین + واریز به حساب ساختگی «هدیه داخلی».

**چرا بد است:** گزارش بانک، تأیید حسابداری، و آمار درآمد را خراب می‌کند؛ کاربر باید دروغ سیستمی بگوید. UX بد و بدهی فنی.

---

## ۴. جریان پیشنهادی کاربر (ایده A)

```
تب فروش‌ها → + ثبت فروش → انتخاب مشتری
        ↓
مرحله ۱: محصول
  - انتخاب محصول
  - نوع: عادی / هدیه
  - اگر هدیه: قیمت قفل روی ۰، فیلد «دلیل هدیه» ظاهر می‌شود
        ↓
مرحله ۲:
  - عادی → واریز(ها)
  - هدیه → خلاصه: «بدون دریافت وجه · تاریخ هدیه · دلیل»
        ↓
دکمه: ثبت هدیه
        ↓
حسابداری: کارت با بج «هدیه» → تأیید / رد
        ↓
بعد از تأیید: محصول در مالکیت مشتری + ماتریس/LRFM
```

پیام‌های کمکی کوتاه:

- بالای فرم هدیه: «هدیه بدون دریافت وجه ثبت می‌شود و برای تأیید مالکیت به حسابداری می‌رود.»
- بعد از ثبت: toast «هدیه ثبت شد و در انتظار تأیید حسابداری است.»

---

## ۵. نمایش در جاهای مختلف پنل

| محل | پیشنهاد نمایش |
|---|---|
| تب فروش‌های مشتری | ردیف با بج سبز/خاکستری «هدیه»، مبلغ `۰` یا «—»، وضعیت «هدیه» |
| شیت فروش‌ها | فیلتر «فقط هدایا» / «بدون هدایا»؛ ستون مبلغ برای هدیه `۰` |
| آمار فروش / درآمد | **پیش‌فرض حذف از درآمد**؛ شمارنده جدا: «تعداد هدیه» و اختیاری «ارزش کاتالوگ هدایا» |
| حسابداری | صف یا فیلتر «هدایا»؛ دکمه تأیید = «تأیید ثبت هدیه» |
| ماتریس محصول | مثل خرید عادی در مالکیت بیاید؛ tooltip: «از طریق هدیه» |
| LRFM / سطوح | تصمیم کسب‌وکار: آیا هدیه در تعداد خرید شمرده شود؟ پیشنهاد پیش‌فرض: **بله برای مالکیت محصول، خیر برای مبلغ خرید** |

---

## ۶. مدل دادهٔ پیشنهادی (حداقلی)

روی آیتم محصول در `customers.products[]`:

```js
{
  name: "…",
  saleType: "gift",          // یا "standard" (پیش‌فرض)
  price: 0,
  priceLocked: true,
  giftReason: "کمپین نوروز", // اختیاری
  giftDate: "1404/…",        // یا از createdAt
  catalogPriceSnapshot: null, // فاز ۲
  payments: [],              // برای هدیه خالی می‌ماند
  status: "gift",            // جدا از بیعانه/تکمیل
  // soldByPhone, accountingStatus, … مثل قبل
}
```

اعتبارسنجی:

- اگر `saleType === 'gift'` → `price` باید ۰ باشد، `payments` خالی، واریز الزامی نیست
- اگر `saleType === 'standard'` → همان قواعد فعلی (`price > 0` و واریز معتبر)

---

## ۷. تصمیم‌های باز برای محصول

قبل از پیاده‌سازی بهتر است این‌ها را قطعی کنید:

1. **آیا هدیه نیاز به تأیید حسابداری دارد؟**  
   پیشنهاد: بله (جلوگیری از ثبت اشتباه مالکیت)، با UI ساده‌تر از فروش پولی.
2. **آیا هدیه در سطح برنزی/نقره‌ای LRFM حساب شود؟**  
   پیشنهاد: در «داشتن محصول» بله؛ در «جمع مبلغ خرید» خیر.
3. **آیا چند بار هدیهٔ همان محصول به یک مشتری مجاز است؟**  
   پیشنهاد: مثل فروش عادی مجاز، ولی با هشدار «این مشتری از قبل این محصول را دارد».
4. **آیا انبار/ارسال فیزیکی برای هدیه لازم است؟**  
   اگر بله، همان فیلدهای آدرس/کدپستی فروش فیزیکی روی فرم هدیه هم بماند.

---

## ۸. جمع‌بندی و اولویت

| اولویت | کار | چرا |
|---|---|---|
| ۱ | ایده A: سوییچ «فروش هدیه» + قیمت ۰ قفل + بدون واریز | بهترین تعادل UX و هزینه پیاده‌سازی روی مدل فعلی |
| ۲ | بج و فیلتر هدیه در لیست فروش / حسابداری / آمار | جلوگیری از آلودگی گزارش درآمد |
| ۳ | فیلد دلیل هدیه + تاریخ | شفافیت عملیاتی و پشتیبانی |
| ۴ | ایده B به‌عنوان CTA دوم (اختیاری) | کشف‌پذیری بیشتر |
| ۵ | ایده C: ارزش کاتالوگ هدیه | وقتی مالی بخواهد هزینه فرصت را ببیند |
| — | ایده E (واریز جعلی) | رد شود |

**پیشنهاد نهایی:** فاز اول را با **ایده A** بسازید؛ هدیه همان خط فروش باشد با `saleType: 'gift'`، قیمت صفرِ رسمی، بدون واریز، تأیید حسابداری برای مالکیت، و حذف از درآمد در آمار. این هم نیاز «مشتری محصول را دارد» را پوشش می‌دهد، هم دروغ قیمت ۱ تومانی را از بین می‌برد، هم مسیر ذهنی فروشنده را نزدیک فرم فعلی نگه می‌دارد.
