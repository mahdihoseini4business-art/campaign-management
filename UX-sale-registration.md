# بهینه‌سازی UX/UI ثبت فروش

بررسی فرم فعلی داخل پنل مشتری (`#detailModal` → بخش «محصولات») و پیشنهادهایی برای کاهش سردرگمی.

---

## ۱. مشکل اصلی چیست؟

ثبت فروش یک **مودال مستقل** نیست؛ بخشی تو در توی «پنل مشتری» است که همزمان اطلاعات پروفایل، LRFM، پیگیری، یادداشت و فروش را نشان می‌دهد. کاربر برای یک کار ساده (ثبت یک فروش) با یک صفحهٔ شلوغ روبه‌رو می‌شود.

علاوه بر این:

| واقعیت فعلی | حس کاربر |
|---|---|
| دکمه می‌گوید «افزودن محصول» | «من می‌خواستم فروش ثبت کنم» |
| ذخیره با blur خودکار است | «کجا ثبت شد؟ باید ذخیره اطلاعات را بزنم؟» |
| فیلدها در یک ردیف فشرده چیده شده‌اند | «کدام فیلد الزامی است؟ کجا اشتباه کردم؟» |
| دکمه «واریز بعدی» گاهی disabled است | «چرا کار نمی‌کند؟» (فقط `title` توضیح می‌دهد) |
| قیمت بعد از اولین ذخیره قفل می‌شود | «اشتباه زدم، چطور درستش کنم؟» |

---

## ۲. پیشنهاد معماری جریان (اولویت بالا)

### ۲.۱ جدا کردن «ثبت فروش» از «پنل مشتری»

الان همه چیز در یک مودال ۶۴۰px اسکرول می‌شود. پیشنهاد:

**گزینه A — مودال اختصاصی ثبت فروش (پیشنهاد اصلی)**  
از تب فروش‌ها / لیست مشتریان، یک CTA واضح:

> **+ ثبت فروش**

باز می‌شود مودالی با عنوان «ثبت فروش — نام مشتری» که فقط این‌ها را دارد:

1. انتخاب / تأیید مشتری (اگر از قبل باز نشده)
2. محصول + قیمت
3. واریز(ها)
4. دکمهٔ صریح «ثبت فروش» یا «ثبت واریز»

پنل مشتری برای ویرایش پروفایل، پیگیری و مشاهدهٔ تاریخچه بماند؛ فروش‌های قبلی را خلاصه نشان دهد و لینک «ثبت فروش جدید» بدهد.

**گزینه B — تب‌بندی داخل همان مودال** ✅ پیاده‌سازی‌شده  
اگر جدا کردن پرهزینه است:

- تب «اطلاعات»
- تب «فروش‌ها» (فعال وقتی کاربر برای فروش آمده)
- تب «پیگیری‌ها»

حداقل ذهن کاربر روی یک کار تمرکز می‌کند.

> **وضعیت فعلی:** پنل مشتری تب‌بندی شده است. تب پیش‌فرض از شیت فعال اپ استنباط می‌شود (فروش‌ها/حسابداری/ارسالی‌ها → فروش‌ها، فالوآپ‌ها → پیگیری‌ها، بقیه → اطلاعات). با رفرش داخل مودال، تب جاری حفظ می‌شود.

**گزینه C — حالت متمرکز**  
با کلیک «افزودن محصول»، بقیهٔ بخش‌های مودال جمع شوند (collapse) و فقط بلوک فروش باز بماند؛ با «بازگشت به پنل» دوباره کامل شود.

---

## ۳. شفاف‌سازی زبان و CTAها ✅ (جزئی)

| الان | پیشنهاد | وضعیت |
|---|---|---|
| محصولات | فروش‌ها / فاکتورها | ✅ تب + empty state |
| + افزودن محصول | + ثبت فروش | ✅ |
| + ثبت واریز بعدی | + افزودن واریز (مانده: …) | ✅ |
| ذخیره اطلاعات (فوتر) | ذخیره اطلاعات مشتری | ✅ |
| بیعانه / تکمیل | وضعیت پرداخت: ناقص / تسویه‌شده (با توضیح کوتاه) | ⏸ عمداً اعمال نشد |

جملهٔ راهنما بالای بخش فروش:

> فروش با تکمیل مبلغ، تاریخ، ساعت و بانک مقصد ثبت می‌شود و برای تأیید به حسابداری می‌رود.

---

## ۴. ساختار فرم: از ردیف فشرده به مراحل واضح ✅

الان یک `product-head-row` و یک `payment-row` با چند input کنار هم در flex هستند؛ روی دسکتاپ هم شلوغ است.

### پیشنهاد چیدمان سه‌مرحله‌ای داخل همان بلوک ✅ پیاده‌سازی‌شده

```
┌─────────────────────────────────────────┐
│ ۱. محصول                                │
│    [انتخاب محصول/باندل ▼]               │
│    قیمت کل *     تاریخ تسویه (اختیاری)  │
│    خلاصه: پرداخت‌شده / مانده / وضعیت    │
├─────────────────────────────────────────┤
│ ۲. واریز ۱                    [در انتظار]│
│    مبلغ *                               │
│    تاریخ *     ساعت *                   │
│    بانک مقصد *                          │
│    نام واریزکننده (اختیاری)             │
├─────────────────────────────────────────┤
│ [+ افزودن واریز برای مانده]             │
└─────────────────────────────────────────┘
```

نکات:

- هر فیلد **لیبل ثابت بالای input** داشته باشد، نه فقط placeholder (placeholder با پر شدن فیلد محو می‌شود و کاربر گیج می‌شود). ✅
- فیلدهای الزامی با `*` و رنگ یکسان؛ اختیاری‌ها با برچسب «اختیاری». ✅
- خلاصه مالی (پرداخت‌شده / مانده) را از ردیف ورودی جدا کنید — مثل یک نوار خلاصه ثابت بالای واریزها. ✅
- دکمهٔ «ثبت فروش» صریح مربوط به بخش ۵ (مدل ذخیره) است و عمداً اینجا اضافه نشده.

---

## ۵. مدل ذخیره: صریح به‌جای blur خاموش

### مشکل
ذخیره روی `onblur` / `onchange` است، ولی فوتر «ذخیره اطلاعات» حس دکمهٔ اصلی را می‌دهد. کاربر مطمئن نیست فروش ثبت شده یا نه. اسپینر هم برای ذخیرهٔ محصول وجود ندارد.

### پیشنهادها (یکی را انتخاب کنید)

**الف) ذخیرهٔ صریح (ترجیح برای کاهش اضطراب)**  
فیلدها محلی بمانند → دکمهٔ «ثبت واریز» / «ثبت فروش» → یک درخواست → toast موفقیت واضح:

> واریز ۱ ثبت شد — در انتظار تأیید حسابداری

**ب) اگر auto-save می‌ماند**  
- نشانگر کوچک: «ذخیره شد ✓» / «در حال ذخیره…» کنار بلوک فروش  
- فوتر را برای فروش گمراه‌کننده نکنید؛ متن را عوض کنید: «ذخیره اطلاعات مشتری»  
- بعد از اولین blur موفق، یک toast کوتاه یک‌باره: «تغییرات فروش خودکار ذخیره می‌شوند»

---

## ۶. کاهش بن‌بست‌ها و قوانین غافلگیرکننده

### ۶.۱ دکمهٔ غیرفعال «واریز بعدی»
الان فقط `title` دارد. بهتر:

- زیر دکمه متن ثابت: «برای افزودن واریز، قیمت کل و فیلدهای واریز فعلی را کامل کنید»
- یا دکمه را فعال نگه دارید و با کلیک، همان validation را با toast + highlight فیلدهای خالی نشان دهید (بهتر از disabled خاموش)

### ۶.۲ قفل قیمت
قفل بعد از اولین مقدار مثبت منطقی است برای حسابداری، ولی UX فعلی غافلگیر می‌کند.

پیشنهاد:

- قبل از قفل: تأیید کوتاه «قیمت بعد از ثبت قابل ویرایش نیست. ادامه؟»
- یا تا وقتی هیچ واریزی «پر» نشده، قیمت قابل ویرایش بماند
- اگر قفل شد: آیکون قفل + tooltip «برای اصلاح با حسابداری / ادمین هماهنگ کنید» (اگر مسیر اصلاح ندارید، حداقل توضیح بدهید)

### ۶.۳ حذف محصول
الان فقط toast می‌گوید حذف ممکن نیست. اگر سیاست کسب‌وکار حذف را ممنوع می‌کند:

- دکمه حذف نشان ندهید
- یا «لغو پیش‌نویس» فقط وقتی هنوز هیچ فیلدی پر نشده

پیش‌نویس خالی (محصول با قیمت خالی و واریز ناقص) نباید مثل فاکتور قطعی حس شود.

### ۶.۴ پیش‌پر کردن تاریخ/ساعت
تاریخ و ساعت الان پر می‌شوند ولی مبلغ و بانک خالی‌اند → ردیف `is-incomplete` زرد می‌شود و حس «نیمه‌کاره و خراب» می‌دهد.

پیشنهاد:

- یا همهٔ الزامی‌ها را خالی بگذارید و placeholder واضح بدهید
- یا یک حالت «پیش‌نویس» با برچسب خاکستری: «در حال تکمیل…» به‌جای هشدار زرد تا وقتی کاربر شروع به ویرایش نکرده

---

## ۷. سلسله‌مراتب بصری و موبایل

### دسکتاپ
- عرض مودال فروش را بیشتر کنید (مثلاً ۷۲۰–۸۰۰px) یا از layout دو ستونه استفاده کنید: راست خلاصه مشتری، چپ فرم فروش.
- فاصلهٔ عمودی بین بخش محصول و واریزها را بیشتر کنید؛ border/پس‌زمینهٔ ملایم برای هر واریز کافی است، ولی فیلدها را داخل کارت شلوغ نکنید.

### موبایل
الان `.product-row` ستونی می‌شود — خوب است — ولی ترتیب فیلدها باید منطقی بماند:

1. محصول  
2. قیمت  
3. مبلغ واریز  
4. تاریخ / ساعت  
5. بانک  
6. واریزکننده  

وضعیت badge را بالای کارت واریز بچسبانید، نه ته ردیف بین فیلدها.

---

## ۸. بازخورد وضعیت و خطا

| موضوع | پیشنهاد |
|---|---|
| وضعیت واریز | کنار عنوان «واریز ۱»؛ رنگ + متن کوتاه |
| رد شده | دلیل رد را کامل و خوانا نشان دهید، نه فقط `title` |
| موفقیت | toast با نام محصول و مبلغ |
| خطای شبکه | الان مسیر `setProducts` برای UI کم‌حفاظت است — پیام «ذخیره نشد، دوباره تلاش کنید» + امکان retry |
| فاکتور بسته | به‌جای فقط badge، کل بلوک را read-only با توضیح یک‌خطی کنید |

---

## ۹. میان‌برهای جریان کاری فروشنده

فروشنده معمولاً این مسیر را تکرار می‌کند: مشتری → محصول → قیمت → یک واریز → تمام.

پیشنهادهای سرعت:

1. **ثبت سریع فروش:** یک فرم فشرده برای حالت «یک محصول + یک واریز کامل»؛ واریزهای بعدی اختیاری پشت «جزئیات بیشتر».
2. **پیشنهاد مبلغ مانده** هنگام افزودن واریز (الان هست — خوب نگه دارید و واضح‌تر نشان دهید).
3. **فوکوس خودکار** روی اولین فیلد خالی بعد از «ثبت فروش» / «افزودن محصول».
4. **ورود با کیبورد:** Enter روی فیلد آخر = ثبت؛ Tab顺 منطقی بین مبلغ → تاریخ → ساعت → بانک.
5. اگر از تب «فروش‌ها» می‌آید، مستقیم مودال فروش باز شود، نه کل پنل مشتری.

---

## ۱۰. نقشهٔ اولویت پیاده‌سازی

### فاز ۱ — کم‌هزینه، اثر بالا (۱–۲ روز)
- تغییر برچسب‌ها: محصولات → فروش‌ها، افزودن محصول → ثبت فروش  
- لیبل ثابت بالای فیلدها  
- توضیح زیر دکمهٔ disabled  
- نشانگر «ذخیره شد» برای auto-save  
- اصلاح متن فوتر  
- highlight فیلدهای ناقص با پیام مشخص، نه فقط border زرد

### فاز ۲ — ساختار فرم (۳–۵ روز)
- چیدمان عمودی مرحله‌ای محصول / واریز ✅  
- نوار خلاصه مالی جدا ✅  
- تأیید قبل از قفل قیمت  
- مدیریت پیش‌نویس خالی (حذف یا عدم ذخیره تا تکمیل حداقلی)

### فاز ۳ — جداسازی جریان (۱–۲ هفته)
- مودال یا تب اختصاصی «ثبت فروش»  
- CTA از تب فروش‌ها  
- ذخیرهٔ صریح با دکمهٔ ثبت  
- حالت ثبت سریع یک‌واریزی

---

## ۱۱. معیار موفقیت بعد از تغییر

قبل از بازنویسی بزرگ، این‌ها را اندازه بگیرید (حتی دستی با ۳–۵ فروشنده):

1. زمان میانگین ثبت یک فروش ساده (یک محصول، یک واریز)
2. تعداد خطا / ویرایش دوبارهٔ قیمت بعد از قفل
3. سوال «آیا ذخیره شد؟» یا کلیک بیهوده روی «ذخیره اطلاعات»
4. رها کردن فرم نیمه‌کاره (محصول با واریز ناقص)

هدف واقع‌بینانه فاز ۱+۲: ثبت فروش ساده زیر ۳۰–۴۵ ثانیه بدون احساس «گم شدن در پنل مشتری».

---

## ۱۲. جمع‌بندی پیشنهاد من

بهترین مسیر از نظر UX:

1. **جدا کردن ذهنیت «فروش» از «پنل مشتری»** (مودال/تب جدا یا حداقل collapse متمرکز)
2. **زبان درست:** ثبت فروش، نه افزودن محصول
3. **فرم عمودی با لیبل** به‌جای ردیف فشردهٔ placeholderمحور
4. **بازخورد ذخیرهٔ واضح** (صریح یا auto-save با نشانگر)
5. **قوانین سخت (قفل قیمت، disabled، حذف) را قابل‌فهم کنید** قبل از اینکه کاربر گیر کند

اگر بخواهی، مرحلهٔ بعد می‌تواند طراحی wireframe همان مودال جدید یا پیاده‌سازی فاز ۱ روی کد فعلی باشد.
